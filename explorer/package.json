{
  "name": "@block-genomics/og-images",
  "version": "1.0.0",
  "private": true,
  "description": "Dynamic OG image generation for Block Genomics social sharing",
  "type": "module",
  "scripts": {
    "generate": "tsx generate-og.ts",
    "generate:block": "tsx generate-og.ts --type block --height 500000",
    "generate:agent": "tsx generate-og.ts --type agent --id satoshi_spirit",
    "build": "tsc",
    "preview": "open output/",
    "prepublishOnly": "echo '[block-genomics] this package is a deploy target, not an npm package. Refusing to publish.' && exit 1"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.0",
    "satori": "^0.12.0",
    "sharp": "^0.35.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
