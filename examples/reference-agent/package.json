{
  "name": "nexus-reference-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The hello-world Nexus agent: load a keypair, prove block ownership (BIP-322), register, heartbeat, long-poll the event stream, and revoke gracefully on shutdown — built on the block-genomics-connect SDK.",
  "scripts": {
    "start": "tsx src/index.ts",
    "keygen": "tsx src/keygen.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "1.9.7",
    "@noble/hashes": "1.8.0",
    "@scure/btc-signer": "2.4.1",
    "block-genomics-connect": "^0.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "tsx": "^4.7.1",
    "typescript": "^5.4.3",
    "vitest": "^3.2.7"
  },
  "license": "MIT"
}
