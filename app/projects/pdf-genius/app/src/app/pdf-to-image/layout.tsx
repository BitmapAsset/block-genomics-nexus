import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'PDF to Image — Convert PDF to JPG/PNG Free | PDF Genius',
  description: 'Convert PDF pages to high-quality JPG or PNG images for free. No upload needed — files stay in your browser.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
