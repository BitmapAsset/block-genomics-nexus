import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Ask Your PDF — Chat with Any Document | PDF Genius',
  description: 'Upload a PDF and ask questions about it using AI. Get instant answers from your documents.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
