import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Split PDF — Extract Pages Free Online | PDF Genius',
  description: 'Split PDF files or extract specific pages for free. All processing happens in your browser.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
