import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Merge PDF Files Online — Free, No Upload Required | PDF Genius',
  description: 'Combine multiple PDF files into one document for free. All processing happens in your browser — your files never leave your device.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
