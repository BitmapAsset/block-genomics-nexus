import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Image to PDF — Convert JPG/PNG to PDF Free | PDF Genius',
  description: 'Convert images to PDF for free. Support for JPG, PNG, WebP. All processing in your browser.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
