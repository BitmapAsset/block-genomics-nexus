import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Watermark PDF — Add Text Watermark Free | PDF Genius',
  description: 'Add text watermarks to your PDF for free. Customize position, opacity, rotation, and font size.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
