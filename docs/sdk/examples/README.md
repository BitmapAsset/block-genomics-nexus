# SDK Examples

These examples are starter files for external builders. They are intentionally renderer-neutral and can be adapted to Three.js, React Three Fiber, Unity, Unreal, Godot, or agent simulation clients.

- [world-builder.ts](world-builder.ts) - minimal TypeScript client for reading and mutating verified block world state.

The examples assume the caller already has a wallet adapter capable of signing a message.
